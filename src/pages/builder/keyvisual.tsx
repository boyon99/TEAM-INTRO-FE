import EditBuilder from "@/components/builder/LeftPanel";
import Layout from "@/components/builder/Layout";
import React, { useState } from "react";

export default function KeyVisual() {
  return (
    <div>
      <Layout>{<h1>KeyVisual</h1>}</Layout>
    </div>
  );
}
